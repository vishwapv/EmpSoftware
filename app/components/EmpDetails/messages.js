/*
 * EmpDetails Messages
 *
 * This contains all the text for the EmpDetails component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.EmpDetails';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EmpDetails component!',
  },
});
