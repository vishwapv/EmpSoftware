/*
 * AddDetails Messages
 *
 * This contains all the text for the AddDetails component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AddDetails';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddDetails component!',
  },
});
