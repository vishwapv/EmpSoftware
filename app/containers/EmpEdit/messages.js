/*
 * EmpEdit Messages
 *
 * This contains all the text for the EmpEdit container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.EmpEdit';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EmpEdit container!',
  },
});
